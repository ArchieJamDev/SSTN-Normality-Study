# Esqueleto del workflow de simulacion (ver notes/DESIGN.md secciones 4, 7 y 10).
# El job calibrate-null se elimino — resulto innecesario (ver DESIGN.md
# seccion 10): sstn::sstn() calibra internamente en cada llamada, rapido.

name: Simulacion SSTN

on:
  workflow_dispatch: {}  # disparo manual mientras se dimensiona el diseño

jobs:
  # Piloto de tiempos (ya corrido con exito, ver notes/pilot_sstn_timing.txt
  # commiteado en el repo) — se deja para poder re-correrlo si cambia algo
  # (version de paquetes, runner, etc.)
  pilot-timing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: r-lib/actions/setup-r@v2
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::sstn
            any::nortest
            any::moments
            any::tseries
            any::fBasics
            any::gld
            any::SimMultiCorrData
      - name: Correr piloto de tiempos SSTN
        run: Rscript R/00b_pilot_sstn_timing.R
      - uses: actions/upload-artifact@v4
        with:
          name: pilot-sstn-timing
          path: notes/pilot_sstn_timing.txt

  # Sanity check de la bateria completa (10 pruebas) sobre datos de juguete,
  # antes de correr la simulacion en serio — confirma que las 9 pruebas
  # clasicas + SSTN funcionan todas juntas sin errores de dependencias.
  battery-sanity-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: r-lib/actions/setup-r@v2
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::sstn
            any::nortest
            any::moments
            any::tseries
            any::fBasics
            any::gld
            any::SimMultiCorrData
      - name: Correr sanity check de la bateria de 10 pruebas
        run: Rscript R/08_run_battery.R

  # TODO: job "simulate" con strategy.matrix (familia x n, o dataset x subescala).
  #       Cada shard escribe su propio CSV de resultados como artifact.
  #
  # TODO: job final "aggregate" que descarga todos los artifacts del matrix y
  #       corre R/99_aggregate_results.R.
